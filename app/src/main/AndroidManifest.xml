<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bossnotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmployeeActivity"></activity>
        <activity android:name=".AddBoss" />
        <activity android:name=".AddEmployee" />
        <activity android:name=".PageBoss" />
        <activity android:name=".PageEmployee" />
        <activity android:name=".sqlEmployee" />
        <activity android:name=".Employeeregister" />
        <activity android:name=".Bossregister" />
        <activity android:name=".sqlBoss" />
        <activity android:name=".loginbossActivity" />
        <activity android:name=".loginemployeeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>